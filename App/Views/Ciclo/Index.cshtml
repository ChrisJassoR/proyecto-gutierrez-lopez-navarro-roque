@model IEnumerable<App.Models.DataBaseModel.Ciclo>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.inicio)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.fin)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.inicio)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.fin)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.cicloClave">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.cicloClave">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.cicloClave">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
